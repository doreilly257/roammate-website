# Release prerequisite refresh — September 15, 2026

**Scope:** Root-supplied read-only provider observations and attributed owner evidence. The report author inspected/copied the compact Apple receipt and performed no fresh scans or network queries. No release, upload, deployment, personal-record query or production mutation was performed by this refresh.

## Fresh root provider observations

| Surface | Root observation today | Limit |
| --- | --- | --- |
| Routed backend | Latest deployment `4be27a68`, September 10; version `b100e60a` at 100% | Unchanged release; later source contracts are not thereby deployed. Backend source resides in `roammate-app-ios/api`, not a separate backend repository |
| Public Pages | `d1d8272a`, source `a95b835c` | Unchanged control-plane identity, not every-edge rendered verification |
| Admin Pages | `c185fa92`, source `6f4cf940` | Does not attest encrypted scoped-key equality or authenticated acceptance |
| Public Apple listings, US/GB/AU/CA | All show **2026.3.7**, release date **2026-09-01T16:09:53Z** | Public listings do not establish private App Store Connect editability |

In each of the four English Apple listings, the older join-in-a-tap and help-a-tap-away wording remains present; both approved replacement statements are absent. All four differ from the corrected local descriptions. The [compact receipt](2026-09-15-public-apple-refresh.json) records only country/version/date and comparison booleans, not personal data. Exact query times are not included in that supplied receipt; this report does not invent them.

This directly preserves the publication gap for `g1v` and the associated promise-audit dependency `do5`. It does not justify repeating already completed local corrections.

## Owner source and historical metadata evidence

- Root observed iOS HEAD `b599a33` with September 15 authentication source work, and Android HEAD `29149ebb` with a September 15 authentication plan. These are source/planning advances, **not released-build proof**.
- iOS commit `ef33392` landed the four approved English corrections and the September 14 reconciliation. That report compared 72 fields: **68 matched / 4 approved differences**. Four keyword fields exceeded the **100 UTF-8-byte** limit. Of 144 screenshots, **105 matched local assets / 39 were unmatched**; “unmatched” does not establish that the screenshots are wrong.
- The same owner's September 14 check found no editable version **at that check's time**. It is not a fresh App Store Connect observation today. User-selected target **2026.3.8** still needs current editability/resource validation; target identity is not upload/submission approval.
- Current tool metadata available to root lacked the required Node REPL browser and App Store Connect tools. Tool absence is not evidence the user's browser is signed out, the account lacks rights, or an editable version still does not exist. Root continued local tool inspection rather than asking the user to guess unknown deployment facts.

## Attributed owner telemetry evidence — not a new root query

Android commit `d2d6a86d` includes a September 15 owner MCP error report with **146 breadcrumbs**, including **123 socket** and **13 upload** entries. The remaining signatures still require lifecycle and expected-error classification before behavior/logging changes. These are breadcrumb counts, **not people, affected-user counts or independent incidents**.

The report's production correlation points to iOS version **2026.3.7**; repository location does not make these Android release events. Root did not rerun the owner query or inspect underlying personal records for this refresh. No trace IDs are copied here. An empty exception list does **not** prove health or absence of user-visible failures.

## Disposition

`g1v`, `do5` and store-handoff acceptance remain blocked on their actual publication/release requirements. Backend/admin contract and scoped-key prerequisites remain separate. Local fixes, an owner plan, historical reconciliation and public version lookup must not be substituted for a verified deployed contract, current editable target, approved upload or release-aligned event delivery.

The useful next checks are bounded discovery of existing authorized read-only tooling and current target/resource validation when accessible—not another local rewrite or an inferred release. No approval boundary is expanded by this report.
