# Blog promise audit — 13 September 2026

## Final root integration verification

After the final reviewed copy edits, the supervising session's combined local
gate command exited **0**: `npm run validate`; `npm test` (**348 tests across
34 files**, 6.18 s); `npx astro check` (**149 files, 0 errors, 0 warnings, 83 hints**);
`npm run build` (**3,505 pages**, Astro phase 35.57 s, **587 authorized indexed
articles**, index and built-link checks passed); and the **662-file** known-pattern
claims check. These are complete-suite totals, not counts of new copy tests or
individually certified claims; the staged focused results below remain historical.

Root inspected final rendered output: all **eight** corrected comparison pages
showed `Optional identity verification`; FAQ contained **four** and safety **two**
instances of the scoped block-notification statements across JSON-LD and visible
copy, with no old two-taps/immediate wording on those pages. Checked how-it-works,
about and press output no longer contained the old all-three/every-compatible
guarantees. Built `llms.txt` and `llms-full.txt` were byte-identical to their
reviewed `public/` sources.

The same output passed exact-byte hashing for all **128 Pagefind manifest entries**
(126 index, 2 filter; 11,831-byte manifest); sitemap/RSS had no search assets and
the transient search-authorization manifest was absent. These are local integration
results, not publication, current competitor/store validation or certification of
all **295** historical claim candidates.

Final-build browser repetition also passed in Chromium **153.0.8010.37**: healthy
Bangkok search returned **73 results**, showing **10**; malformed HTTP-200 index
and filter cases each failed closed with **0 items**, then same-page Retry restored
73 results without CSP violations or query-bearing URLs. An initial stale fixture
child survived its wrapper's Ctrl-C and held an old manifest across the rebuild;
after stopping the identified server and restarting on a verified-free port,
the successful repeat used the new build's assets. See the
[integrity report](2026-09-13-pagefind-integrity.md) for exact traffic evidence and
the caution to restart the fixture whenever `dist` changes. No deployment occurred.

## Scope and method

Local-source continuation of Beads `roammate-website-do5`, not a live-site or store
certification. Enumerated product-bearing prose before applying known-false rules.
No browser, live API, personal records, owner writes, store uploads or deployment.

- Inventoried all **127 JSON blog posts**. Inspected `description`, `intro` and each
  `sections[].content` field: **556 fields**, with array content joined for screening.
  **71 fields in 22 posts mentioned roammate before these edits**; these are candidate prose fields,
  **not 71 independently verified claims**. Byline, related-slug and image-path
  matches were excluded. Reviewed the candidate passages in context, including
  adjacent pronouns and negative capability statements.
- Enumerated all **64 roammate value cells across nine static comparison tables**.
  `src/pages/blog/[slug].astro` excludes these nine slugs; each static page loads
  the JSON through `src/lib/blog-data.ts` and appends its own table. Both surfaces
  matter: auditing JSON alone misses rendered claims.
- The original **295 candidates in 41 files** and original **39 blog claims in
  19 posts** are historical do5 figures, not counts reverified by this pass.
  This pass does not establish all current website, competitor or store claims.

Paths below are relative to `roammate.com/` unless otherwise stated.

## Established truth used

The do5 owner-confirmed record establishes matching by travel style, daily budget
bracket and destination overlap; optional government-ID/liveness verification with
a profile badge; in-app chat, block/report; nominated emergency contacts and SOS;
and free availability on iOS and Android. Its later notes explicitly correct the
initial suspicion about reviews and record the moderation commitment as confirmed.
The same optional-verification evidence is recorded in
`scripts/check-claims.mjs` (`verification-is-mandatory`: inert mandate/default flag),
and the existing verification/Android copy regressions preserve prior approved
website corrections. These are established project records, not a new production
feature-flag check.

Repository-local `.tmp/appstore-drafts/APPLY.md:32-62` records real-time chat,
trip memories, bucket lists and groups, plus **shipped, end-to-end-tested reviews**
(double-blind release, invalid-review rejection and rating updates). It also records
the disabled concierge and missing read-receipt UI. That ignored artifact is
historical evidence, not an assertion that current store metadata was re-exported.
No SOS, badge or group prohibition was reinstated, and no inference was made from
missing analytics events. Current metadata acceptance stays with the owning apps.

## Proven false and corrected locally

| Original claim | Exact source | Evidence and local correction |
| --- | --- | --- |
| Verification: `Yes (all users)` | `src/pages/blog/roammate-vs-{backpackr,bumble-bff,facebook-travel-groups,gaffl,hostelworld,meetup,tourlina,travello}.astro`, verification table row | Eight claims contradict established optional verification. All eight now say `Optional identity verification`. Couchsurfing already said `Optional, badged` and was unchanged. |
| Potential companion `has verified their identity and travel history before you agree to anything` | `src/content/blog/why-roammate-perfect-companion.json`, `sections[0].content` | Guaranteed verified identity contradicts optional verification; no travel-history attestation evidence was supplied. Replaced the compound guarantee with optional government-ID/liveness check, visible badge and profile details, explicitly not a safety/trustworthiness guarantee. This does not assert that history attestation was proven absent. |
| `roammate's Android version is in development` | `src/content/blog/roammate-vs-hostelworld.json`, `sections[1].content` | Contradicts established shipped Android availability and the page's own iOS/Android table. Removed the stale clause and stated free/no-ads availability on iOS and Android. Competitor claims unchanged. |

**Ten false/overpromising instances corrected across ten content files.** Local
implementation only; none is claimed published by this audit. No URL, page count,
robots or llms change is needed for these in-place corrections; build verification
belongs to root integration.

## Claim-category dispositions

| Category | Disposition and representative sources |
| --- | --- |
| Compatibility matching, travel-specific profiles and planning before arrival | Supported at the capability level by established owner confirmation. Repeated throughout the 22-post inventory and all nine tables. Exact “only”, “all three before any profiles” and comparative superiority formulations are not independently validated algorithm guarantees. |
| Free, iOS/Android, no premium tier, no ads | Established project truth; corrected the additional Hostelworld contradiction. This is not a fresh App Store/Play listing check. |
| Optional identity verification and visible badge | Established; corrected eight mandatory table claims and the compound companion guarantee. `is-roammate-safe.json` explicitly permits unverified messaging. |
| In-app chat and block/report | Capability established. `free-couchsurfing-alternatives-2026.json` and `is-roammate-safe.json` contain stronger surface/retention promises requiring the qualifications below. |
| Emergency contacts and SOS | Established capability; retained in hiking, India and safety-app posts. No claim of SMS or professional rescue added. |
| Companion matching rather than accommodation/trek operation | Consistent with established product role; accommodation denial retained in costs, hiking, Middle East, India, Southeast Asia and accommodation comparisons. A blanket denial of every possible booking capability is a different claim. |
| Reviews, trip memories, bucket list, groups | Existing evidence supports capability. Do not remove reviews because an old event taxonomy omitted them; do not resurrect retired rules. Negative comparisons may need updating as these features evolve. |
| Competitor pricing, features, demographics and market superiority | Outside this roammate capability check; not verified without permitted current external research. Left unchanged. |

## Unsupported promises qualified; remaining evidence limits

These are evidence findings attached to do5, not a separate task tracker.

- **Age collection — unsupported absolutes qualified locally:**
  `best-solo-travel-apps-over-40.json`, intro previously said `We hold no age data
  on our own users`; `sections[0].content` said `We do not collect age`. Current owner source
  `/Users/doreilly/Work/roammate-app-ios/api/src/routes/profileSetup.ts:75-106`
  accepts private `dateOfBirth`, and `api/src/services/profileSetup.ts:443-450`
  derives age. This is a concrete source contradiction requiring owner release
  confirmation; source presence alone does not prove the route is currently
  deployed/reachable. Both absolutes were replaced with strictly article-scoped
  statements: this post presents no roammate age breakdown and makes no inference
  about its users. These **two qualifications are separate from the ten proven
  false/overpromising corrections** above; they assert neither current collection
  nor noncollection. No claims-guard rule or privacy-policy change was made.
  A new regression failed before editing and passed afterward. No personal records
  were read. The same post's `sections[2].content` previously said `no age filter,
  no gender filter`; that now describes this article's focus on budget, travel
  style and destination overlap instead. Filter availability was not inferred
  from DOB storage or age eligibility checks.
- **Exact location and retained information — qualified locally:**
  `is-roammate-safe.json`, `sections[1].content` previously promised cutting
  contact `without them retaining any of your information` and that GPS,
  accommodation address and movements stay private. Copy now explains that
  blocking cannot erase already shared information or prevent screenshots, and
  advises reviewing permissions/settings and sharing fewer details. No opposite
  location-disclosure or retention-policy assertion was added.
  `best-safety-apps-solo-travel-2026.json`, `sections[5].content` replaces `Nobody
  sees your live position on a map` with permissions/sharing precautions.
  Exact one-tap controls were subsequently qualified as described below;
  established block/report capability alone does not prove tap counts.
- **Blanket negative capabilities — prose qualified locally:**
  `roammate-vs-travello.json`, `sections[0].content` previously said `roammate does
  one thing` and `There's no social feed, no activity booking, no inspiration
  content`. It now describes the established companion-matching purpose, without
  claiming those other capabilities present or absent. Existing groups/memories
  evidence does not prove a social feed or booking surface is shipped.
  The Travello activity-booking table and
  `couchsurfing-vs-hostelworld-vs-roammate.json`'s `no booking` need product-scope
  clarification rather than guesses from route filenames.
- **Reference/insurance/liability denial — qualified locally:**
  `best-apps-to-find-a-hiking-or-trekking-partner.json`, `sections[0].content`
  previously said `There is no fitness check, no reference system, no insurance
  and no liability`. It now distinguishes identity badges and traveller reviews
  from a trekking-fitness assessment and recommends qualified guidance and
  suitable travel insurance. Reviews remain established; no legal liability or
  platform insurance guarantee is made.
- **Outcome/superiority wording:** The specific “deepest compatibility matching”
  and “faster and more reliable” passages were subsequently qualified below.
  Exact-example matches elsewhere in companion prose are not demonstrated by
  capability existence. Neither population size, successful matching rates nor
  competitor effectiveness was measured here.

The five additional risk-promise passage qualifications above are **not added to
the ten proven false/overpromising instances**. They join the two age-data
qualifications as conservative wording improvements where exhaustive capability,
privacy or legal assertions were not justified. No new broad known-false rules,
competitor-fact changes or privacy-policy edits were made.

## Reproducible regression evidence

Before content/guard changes, new tests failed for all eight table cells, the
companion guarantee and Hostelworld development clause. Guard fixtures independently
failed because the existing regexes returned success for both verification-table
labels and the compound guarantee. An allowed-copy fixture was adjusted to avoid an
unrelated pre-existing cross-sentence regex match, then passed before implementation.

After changes:

```text
npx vitest run scripts/__tests__/verification-copy.test.ts \
  scripts/__tests__/android-availability-copy.test.ts \
  scripts/__tests__/claims-guard.test.ts \
  scripts/__tests__/blog-demographics-copy.test.ts \
  scripts/__tests__/blog-promise-qualifications.test.ts
5 files passed; 26 tests passed
node scripts/check-claims.mjs --all
claims: clean (662 files scanned)
```

The guard now detects the verification-label/roammate-value cell pair without
consuming the competitor's adjacent cell, plus the exact guaranteed-companion
variant. Positive coverage retains optional verification, verified badges, reviews,
SOS, groups and a competitor's all-users cell. A clean 662-file guard remains only
a known-pattern regression result, **not broad audit completion**.
The five risk-promise qualification regressions also failed on the old text before
the neutral edits and passed afterward. `git diff --check` passed.

## Additional neutral accuracy slice — isolated worktree

The following local-only edits were made in `.worktrees/pagefind-integrity`, not
the main working directory. They are unsupported-promise qualifications, not new
proven-false product-feature findings and not additions to the original ten count.

- `src/pages/how-it-works.astro`: both matching FAQ answers (JSON-LD and visible
  prose) replace `You only see profiles that pass all three compatibility filters`
  with reviewing profiles/discussing plans. The confirmed three matching inputs
  remain, and a regression requires the same qualification on both surfaces.
- `src/pages/faq.astro`: the “find a travel buddy” and matching answers no longer
  guarantee that every surfaced person passes all three filters. They describe
  the confirmed matching inputs and advise discussing plans; this shared FAQ data
  supplies the page's structured and visible answers.
- `src/content/blog/{roammate-vs-gaffl,roammate-vs-meetup,roammate-vs-travello,
  best-travel-companion-apps-2026}.json`, each `sections[0].content`: remove
  conjunctive all-three/profile-pass guarantees in favor of matching inputs and
  reviewing potential companions. The previous verification regression was
  updated so it no longer requires the unsupported Travello all-three sentence.
- `src/pages/travel-companion-apps.astro`: both summary `strengths` and visible
  safety prose now describe in-app report/block tools, without one-tap/every-screen
  promises. `is-roammate-safe.json`, `sections[1].content`, does the same.
- `roammate-vs-tourlina.json`, `sections[1].content`, and
  `best-travel-companion-apps-2026.json`, `sections[0].content`, replace “deepest”
  superiority with descriptive matching purpose. Tourlina's reduced-scrolling
  outcome promise is replaced with profile review advice. `roammate-vs-meetup.json`,
  `sections[1].content`, replaces “faster and more reliable” and guaranteed
  pace/budget alignment with matching inputs and a discussion of plans.

All nine content files retain competitor claims/cells and established reviews,
SOS and groups. No new known-false regex, privacy policy, backend, store or release
claim was added. No URL/page restructuring, production writes, full build or
deployment was performed; root owns final integration gates.

TDD: all **11 new matching-copy tests** and the revised Travello expectation
failed on the prior copy before edits. After edits, the six focused files passed:
`matching-copy-qualifications`, `verification-copy`, `blog-promise-qualifications`,
`blog-demographics-copy`, `android-availability-copy`, `claims-guard` — **37 tests**.
The 662-file claims guard and `git diff --check` also passed.

The follow-on enumeration below supersedes the earlier named-file limit for
identified matching and tap-count promises. Current owner/store release acceptance
is still outside these results; this is not certification of all 295 historical
candidates or all current claims.

## Follow-on enumeration and scattered same-claim sweep

Screened product prose across `src/pages`, `src/components`, `src/layouts`, the blog
corpus and both public llms files for remaining “only”/before-conversation matching,
tap counts, comparative outcomes, market-size and booking negatives. Reviewed hits
in context; generic travel advice and competitor-only factual passages were not
treated as roammate guarantees. This is a broader candidate enumeration, not a count
of every independently verified assertion or a new current-product data export.

Thirty new regressions cover **29 sources** plus paired find-companion FAQ advice.
The initial 29 tests failed before changes; a later safety-header/travel-buddy
extension also failed before its edits. The tests prohibit the identified
unsupported variants, not the underlying matching/safety capabilities.

| Sources (under `roammate.com/`) | Neutral changes |
| --- | --- |
| `public/llms-full.txt:33,36` | Replace all-three hard filtering with confirmed matching inputs and discussing plans. |
| `src/pages/how-it-works.astro:169,185` | Remove “every suggestion is genuinely compatible” and two-tap promise. |
| `src/pages/safety.astro:63,71,118-119,183,187` | Qualify both paired JSON-LD/visible report/block answers, replace risk-reduction heading with “Discuss your travel expectations”, describe matching inputs rather than guaranteed screening. |
| `src/pages/faq.astro:15,55` | Replace two-tap promises with in-app blocking/reporting. |
| `src/pages/find-travel-companion.astro:39,126,201` | Qualify pre-screened compatibility and guaranteed alignment; structured/visible FAQ advice remains identical. |
| `src/pages/what-is-a-travel-companion/index.astro:53,94,125,157` | Replace most-compatible/pre-conversation screening claims with matching inputs and discussion. |
| `src/pages/statistics/index.astro:340`, `src/pages/meet-travelers.astro:76` | Replace pre-conversation compatibility promises with a starting point for discussing plans. |
| `src/pages/travel-companion-apps.astro:74,197-200,324-326` | Remove up-front screening guarantees while preserving established matching inputs and competitor-only passages. |
| `src/pages/what-is-adventure-travel/index.astro:121` | Replace guaranteed same-style/incompatible-match removal with discussing activity interests, experience and fitness. |
| `src/pages/what-is-budget-travel/index.astro:120`, `src/pages/how-much-does-it-cost-to-backpack-southeast-asia/index.astro:162` | Replace strongest-predictor/frictionless/savings guarantees with agreeing spending expectations. |
| `src/pages/travel-buddy-app.astro:121,125,129,133` | Replace guaranteed style/budget alignment, number-one friction cause and pre-arrival connection with discussion advice; optional badge is not guaranteed identity/history knowledge or safety. |
| `src/pages/blog/roammate-vs-gaffl.astro:93` | roammate's “Before conversations” cell becomes “Travel profile inputs”; competitor cell unchanged. |
| `src/content/blog/roammate-vs-backpackr.json`, sections 0–1 | Replace only-matching travellers and automatic filtering-outcome guarantees with potential companions and discussion. |
| `src/content/blog/roammate-vs-bumble-bff.json`, sections 0–1 | Describe matching inputs and ability to start looking before booking, not guaranteed pre-conversation/pre-booking matches. |
| `src/content/blog/roammate-vs-couchsurfing.json`, section 1 | Remove every-profile hard compatibility filter. |
| `src/content/blog/roammate-vs-tourlina.json`, section 0 | Remove guaranteed alignment and unsupported larger-pool/coverage inference from gender openness. |
| `src/content/blog/roammate-vs-gaffl.json`, intro and section 1 | Replace up-front compatibility/faster/fewer mismatches with profile inputs and discussion. |
| `src/content/blog/finding-travel-companions.json`, sections 0–1 | Replace most-purpose-built/mismatch-removal and biggest-predictor assertions. |
| `src/content/blog/why-roammate-perfect-companion.json`, sections 0–1 | Turn Koh Tao/Ha Giang exact-match outcome into example questions; local familiarity is possible, not guaranteed by a match. |
| `src/content/blog/best-apps-meeting-travellers-middle-east.json`, section 1; `best-apps-splitting-travel-costs.json`, section 3; `best-solo-travel-apps-over-40.json`, section 2 | Replace assured budget alignment/already-had-the-conversation wording with discussing costs. |
| `src/content/blog/free-couchsurfing-alternatives-2026.json`, section 6 | Qualify assured matching, every-profile controls and unmeasured strongest-destination claim. |
| `src/content/blog/best-safety-apps-solo-travel-2026.json`, section 5 | State in-app controls without every-profile/conversation availability. |
| `src/content/blog/best-travel-companion-apps-2026.json`, sections 0,4 | Replace unmeasured destination-density and outperforming-platform assertions with availability caution and choosing by purpose. |
| `src/content/blog/roammate-vs-facebook-travel-groups.json`, sections 0–1 | Replace solves-all-three/no-volume-needed outcomes with travel-focused matching and optional identity verification. |
| `src/content/blog/couchsurfing-vs-hostelworld-vs-roammate.json`, section 3 | Replace assured budget/pace alignment and pre-booking match with looking for/discussing potential companions. |

Latest local result: **67 tests passed across seven focused files**, including
`remaining-promise-copy.test.ts`; **662-file known-pattern guard clean** and
`git diff --check` clean. No full build, commit, deployment or new claims-guard
rule was performed by this worker. These are neutral qualifications separate from
the original ten established false/overpromise fixes.

### Final product-scope qualifications and evidence limits

- `how-it-works.astro:185`: replaced trip plans “only shared with matched users,
  never publicly” with reviewing sharing settings/intended audience and only
  sharing necessary details. Current privacy contract was not inferred.
- `src/pages/blog/roammate-vs-travello.astro:62-63`: activity booking `No` requires
  current capability/release evidence; roammate's cell now says `Not assessed here`.
  The `Activity booking` label and competitor `Yes` cell remain unchanged.
- `travel-companion-apps.astro:336-337`: no home-city events calendar and no
  Facebook-scale volume need product/market evidence. Calendar denial is replaced
  with companion-matching focus and checking dedicated accommodation/local-event
  providers. The Facebook-volume comparison remains unchanged in substance by
  explicit review scope; current comparative audience research is not certified.
- `couchsurfing-vs-hostelworld-vs-roammate.json`, section 3: `no hosting, no booking
  and no beds` overextends the established companion-not-accommodation purpose.
  It now advises readers to arrange accommodation separately, avoiding a blanket
  denial of any booking feature. Companion-not-accommodation wording in llms and other
  blogs is supported by the established role and is not itself the same issue.
- `roammate-vs-facebook-travel-groups.json`, section 1: `they see only your travel
  profile` was replaced with reviewing profile/conversation sharing and avoiding
  sensitive details with someone not yet trusted. No universal disclosure or
  opposite-visibility claim is made.

All five final product-scope regressions failed before edits and passed afterward.
Latest final focused run: **72 tests passed across eight files** (the previous
seven plus `product-scope-copy.test.ts`); **662-file known-pattern guard clean**;
`git diff --check` clean. The earlier 67-test result is historical within this
continuation. These five neutral qualifications do not increase the original ten
proven-false/overpromise count. Root owns full rebuild, independent review and
commit/push; no deployment or publication is asserted here.

Independent review found three additional same-claim remnants: the visible intro
at `travel-companion-apps.astro:136-137`, the guaranteed fit in
`best-safety-apps-solo-travel-2026.json` section 5, and the budget-pairing guarantee
at `public/llms-full.txt:50`. All three were qualified with matching inputs and
discussion advice; extended regressions failed before edits, then the same
72-test focused run and 662-file guard passed again. These findings demonstrate
why source enumeration and independent review are necessary beyond a clean guard.

### Final independent review and owner-source reconciliation

The independent reviewer screened **198 sources / 202 candidate snippets** across
pages, components, layouts, blog prose and public llms files. These are screening
counts, **not 202 independently verified product claims**. Its consolidated final
analogue list also identified `Features.astro:16`, `about.astro:222`, and
`press.astro:35` (“every connection is genuinely compatible”); llms introductory
guarantees; the shared GAFFL FAQ's already-fitting trip; meet-travelers paired
icebreaker superiority and no-awkwardness promise; and find-companion exact
savings, all-preferences filtering and guaranteed identity knowledge. All were
qualified with matching inputs, profile review, discussion or conditional savings.
New/extended scoped regressions failed before these edits and passed afterward.

Read-only owner-source follow-up resolved the two remaining identified website
implementation questions without guessing from ticket titles:

- **Verified-only filtering is real; claim retained.** In the iOS repository,
  `roammate/Features/Connect/Views/ConnectView.swift:67` mounts
  `VerifiedOnlyFilterRow`; `ConnectView+VerifiedFilter.swift:10-27` provides the
  toggle/label/accessibility; `ViewModels/ConnectViewModel.swift:35-39,232,289`
  persists/propagates `verifiedOnly`; `Services/UserService.swift:114-142` sends
  the query. Android `ConnectScreen.kt:370-378` and `SearchScreen.kt:95-98` expose
  chips, with `ConnectVerifiedOnlyFilterTest.kt:72,91` covering search/nearby
  propagation. Backend `api/src/routes/users.ts:715-723,751-758` filters search
  and nearby/cache results. `travel-companion-apps.astro`'s badge-filter copy was
  therefore retained. These are implementation refs, not fresh release proof.
- **Block completion and explicit notification are narrower than “immediate”.**
  Backend `api/src/routes/users.ts:1231` awaits
  `api/src/services/moderation.ts:186-213`, inserting `user_blocks` before the
  moderation report. The inspected report path (`moderation.ts:127-180`) writes
  database/R2 records; `users.ts:1233-1287` revokes existing notifications,
  purges/invalidate caches and returns success, without sending a target block
  push/email/event. `api/src/routes/conversations.ts:658-660` denies a blocked
  send with 403. Android `ChatViewModel.kt:971+` and
  `UserProfileViewModel.kt:183-199` update UI after success. FAQ's two answers and
  safety's paired structured/visible answer now state: “Blocking helps you control
  who can contact you. The block action does not send a notification to the blocked
  person.” Two paired regressions failed before qualification. No instant/offline,
  race-free or inability-to-infer-the-block guarantee is made; no dedicated
  negative notification test or fresh deployed-path check was identified.

**Final worker result: 78 focused tests passed across eight files**, the 662-file
known-pattern guard passed, and `git diff --check` passed. Source edits are frozen
for independent final review and root's full tests/Astro/build/hash/render gates.
Earlier test counts above document staged red/green work, not competing final
totals. No worker deployment, upload, backend write or commit occurred.

No identified local copy implementation from this completed own-product sweep is
being parked merely because current store acceptance is external. Original do5
still includes current capability/metadata verification for **12 store locales**:
historical concierge/read-receipt findings and eight unapplied drafts are not
current owner acceptance or a fresh release check. Those owning-project release
results remain required for the broad issue. Optional competitor pricing/features,
market comparisons (including retained Facebook volume wording) and generic travel
research are expressly **not certified**, but are not invented prerequisites for
the original own-app capability audit. Historical 295 candidates are not claimed
reverified by this work.

None of these unknown contracts was declared false from absent events, route
filenames or old tickets. Competitor pricing/features and generic travel research
remain outside current roammate capability verification. Existing reviews, SOS,
badges and groups are not prohibited or removed.

## Candidate-post inventory

All names below refer to `src/content/blog/<name>.json`:

```text
best-apps-meeting-travellers-middle-east
best-apps-splitting-travel-costs
best-apps-to-find-a-hiking-or-trekking-partner
best-safety-apps-solo-travel-2026
best-solo-travel-apps-over-40
best-travel-apps-for-backpacking-india
best-travel-buddy-apps-southeast-asia
best-travel-companion-apps-2026
couchsurfing-vs-hostelworld-vs-roammate
finding-travel-companions
free-couchsurfing-alternatives-2026
is-roammate-safe
roammate-vs-backpackr
roammate-vs-bumble-bff
roammate-vs-couchsurfing
roammate-vs-facebook-travel-groups
roammate-vs-gaffl
roammate-vs-hostelworld
roammate-vs-meetup
roammate-vs-tourlina
roammate-vs-travello
why-roammate-perfect-companion
```
