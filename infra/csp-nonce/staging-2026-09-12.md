# CSP nonce isolated staging evidence — 2026-09-12

## Status and authorization

**Staging verified; NOT production-active.** The user approved isolated staging
only. This report records the supervising agent's validation results, not a
production rollout or a claim that bead `8sw` is resolved. Production deployment
remained `a70bd978-9b79-48e1-98ea-f0e95f62ab50`.

No production deployment, bot/WAF/cache-rule change, paid-plan change, or normal
`deploy.sh` change was made. Account quota/cost review, a Pages failure-policy
decision, and explicit production rollout/rollback approval remain necessary.

## Isolated deployment and resources

| Item | Recorded value |
| --- | --- |
| Pages project / branch | `roammate` / `csp-nonce-review` |
| Preview deployment | `4beb26a0-2416-49d9-a6d7-fc7b3f0bae74` |
| Same-zone staging URL | `https://csp-nonce-review.roammate.com` |
| Deployment URL | `https://4beb26a0.roammate-cs7.pages.dev` |
| New proxied CNAME | `csp-nonce-review.roammate.com` → `csp-nonce-review.roammate-cs7.pages.dev` |
| DNS record ID | `562d67de715a8bd99cc641d2b0912a22` |
| Pages custom-domain ID | `613967e9-658d-4a7e-9f23-6c5aa27c3070` |
| Staging custom-domain status | `active` |
| Production and preview failure policy | Both `fail_open: true` — unchanged; requires review before production rollout |

The middleware host allowlist was explicitly extended only for the approved
staging hostname, with `noindex, nofollow`. Pages custom branch aliases require a
proxied CNAME; see [Cloudflare's custom branch alias documentation](https://developers.cloudflare.com/pages/how-to/custom-branch-aliases/).

Deployment ran from scratch directory
`/var/folders/yb/0nhr_99s3ys4_2ms6pyp_3l40000gn/T/roammate-csp-nonce.sygdc_gs`,
containing a copied build, `functions/`, `_routes.json`, and a staging-only
`_headers` noindex addition. No middleware was copied into normal deployment
autodiscovery paths. Wrangler `4.131.1` on Node `26.8.2` successfully uploaded
3,519 files with 1,346 cached files. Bead `nz1` was closed on this authorized
successful upload evidence; `8sw` remains open.

## Local quality gates

- Staging-host regression: one initial failing test, then **11/11 nonce tests
  passed**; JavaScript type checking passed.
- Full public-site validation: **105 tests passed**, Astro check passed, build
  generated **3,504 pages**, and built internal-link validation passed.

## Live HTTP evidence

A 21-response curl matrix passed, including eight unique HTML response nonces
across repeated GET, HEAD, conditional GET, range GET, 404, home and guide cases.

- HTML was `DYNAMIC` with `no-store`, no ETag or Last-Modified validators, and
  staging noindex headers.
- The trailing-slash redirect remained **308**, without a nonce.
- Image, CSS, JavaScript and font bytes matched the built assets and retained
  immutable caching. A same-connection WebP sequence was **MISS → HIT → HIT**.
- A live Wrangler tail filtered to the review's tagged requests captured
  `/blog/` and `/nonce-routing-control/` (404), both with outcome `ok` and zero
  exceptions. Interleaved image/CSS/font requests produced no Function events.
  This is bounded empirical confirmation of those `_routes.json` exclusions,
  not a guarantee for every asset path; cache hits alone would not prove bypass.
  The tail session was stopped after validation.
- Admin remained **302**, private/no-store; API health remained **200**.
- Production home remained **200**, `DYNAMIC`, and without a CSP nonce.

## Real-browser evidence

The blog and three fresh browser contexts on `/`, `/companions/bangkok/`, and
`/guides/kratie/` verified real, unmocked Cloudflare JavaScript Detections (JSD).

- JSD inline injector and iframe script nonce properties matched the response
  CSP nonce; fresh contexts used distinct nonces.
- JSD main script followed **302 → 200**; the oneshot POST returned **200**.
- There were **zero CSP violations and zero page errors** in these checks.
- PostHog SDK loaded after cookie acceptance; blog search for `Gulf` filtered to
  one result.

Analytics ingestion was intercepted locally to prevent production-data
pollution. These checks establish SDK loading and UI behavior, **not end-to-end
analytics delivery**. Cloudflare JSD traffic was not mocked. No secrets, raw
nonces or cookies are retained in this report.

## Production gates and review limits

Same-zone staging establishes JSD compatibility for the observed requests, not
universal injection on every request or production activation. Review account
traffic and quotas before moving public HTML to per-request Functions. The
Pages Free allowance is **100,000 requests/day shared with Workers**; static
requests that do not invoke Functions are free, per [Pages Functions pricing](https://developers.cloudflare.com/pages/functions/pricing/).

Account subscription reads were unavailable: MCP returned an authentication
error, and the authorized direct API returned **403**. The pricing figures above
are general documentation, not verification of this account's plan, available
headroom or Free eligibility; none should be inferred from the successful upload.

Both production and preview currently have `fail_open: true`. Explicitly decide
whether this policy is acceptable on
quota exhaustion; do not change it without approval. Obtain production rollout
and rollback approval, and recheck the current production deployment before any
rollout. Keep `8sw` open until production resolution is actually verified.

## Cleanup and rollback boundaries

After review, remove **only** the newly created staging DNS record and Pages
custom-domain association identified above. Optionally delete the isolated
preview deployment and its local scratch directory when no longer needed.
Preserve production DNS, the production Pages deployment, existing domains,
bot/WAF/cache settings, and the normal deployment workflow.

If a separately approved future production rollout fails, restore the recorded
last known-good static Pages deployment and verify Function removal and static
security-header restoration. Do not weaken script policy as a fallback.
