# Public nonce: 48-hour operational observation

Date: September 14, 2026. Bead `roammate-website-9lk`. The elapsed-window follow-up is collected; **full cost/latency acceptance is not established**. No configuration, deployment, purge, subscription, alert or billing change was made by this observation.

This report uses root's [sanitized evidence JSON](2026-09-14-nonce-48h-evidence.json), the [first-24-hour report](2026-09-13-nonce-first-24h.md), and the historical [operations review](../../../infra/csp-nonce/operations-2026-09-12.md). The author inspected receipts and independently summed the counts without issuing network requests. Root's collection completed at **17:21:45.902129 UTC**; individual GraphQL query start times are not separately recorded.

## Exact windows and fresh first-day requery

The original rollout second was September 12 at 16:24:40 UTC. Both queries use half-open boundaries (`datetime_geq` / `datetime_lt`):

- First day, **freshly re-queried**: September 12 16:24:40 → September 13 16:24:40 UTC.
- Second day: September 13 16:24:40 → September 14 16:24:40 UTC.

The public production script remains `pages-worker--18564688-production`, mapped by the original tagged production tail. Fresh first-day invocation and CPU values equal the earlier recorded values; this supplies a delayed-ingestion recheck, not a guarantee that future analytics revisions are impossible.

## Invocation, error and CPU observations

| Dataset | Fresh first-day requests | Second-day requests | Reported invocation errors first / second |
| --- | ---: | ---: | ---: |
| Public production Pages script | 8,323 | 9,389 | 0 / 0 |
| All returned Pages Functions groups | 8,561 | 10,187 | 0 / 0 |
| Workers, separate dataset | 12,916 | 7,660 | 0 / 0 |
| Combined account invocation counts | 21,477 | 17,847 | 0 / 0 |

The Pages query returned five first-day groups and three second-day groups, below its limit of 100. The Workers query returned one aggregate row per window. All Pages subrequests total 8,593 / 10,169; the separate Workers totals are 26,499 / 12,478. These are account/workload context, not nonce-attributable charges.

| Public production metric | First day | Second day |
| --- | ---: | ---: |
| CPU p50 | 2,378 μs = **2.378 ms** | 2,453 μs = **2.453 ms** |
| CPU p99 | 6,091 μs = **6.091 ms** | 6,312 μs = **6.312 ms** |
| Subrequests | 8,323 | 9,389 |
| Average sample interval | 1.205882… | 1.246052… |

These are adaptive estimates, not an unsampled request census. Do not multiply returned totals by the sample interval. CPU quantiles are neither mean nor maximum, cannot establish aggregate billed CPU, and are not end-user latency. Zero reported invocation errors does not mean every HTTP status is successful: an expected 404 can have a successful invocation.

The prior report's pre-rollout production Function row was absent, not proof of zero public HTTP requests or a comparable production CPU baseline. Preview CPU is not a replacement baseline. Combined account counts include unrelated Workers/Pages workloads and must not be represented as incremental nonce traffic, savings or costs.

## Bounded public HTTP observations

Root's HTTP receipt is timestamped **17:15:10.250234 UTC**, after the second window ended. Three samples each of `/`, `/about/`, `/faq/`, and `/guides/bangkok/` returned **200**; three samples of `/nonce-observation-20260914` returned the expected **404**.

All **15** samples had `Cache-Control: no-store`, `CF-Cache-Status: DYNAMIC`, a CSP nonce present and no ETag. Only presence/absence and sanitized headers/status/timing were retained; no response body or raw nonce was stored. These samples support the observed nonce/cache guard behavior, not all-route coverage, body/header nonce consistency, nonce uniqueness, static-asset behavior or an all-day HTTP-error census.

Local-client time to headers ranged **397.48–1,005.23 ms**. The earlier report recorded **476.03–2,100.10 ms**, but the observations are not matched by network, edge, client state or representative traffic. Comparing those ranges cannot establish an improvement, regression or nonce-attributable delay. No matched pre-rollout public HTML latency baseline was recovered.

## Deployment and operations context

Root's canonical Pages metadata records deployment `d1d8272a-bf47-4340-bb41-166cc6740d99`, created `2026-09-12T19:14:49.017717Z`, source `a95b835c9dc54fbdba415f9f964fda29b2aa1717`. Canonical control-plane metadata does not identify the effective deployment for every historical request/edge.

The windows include release/rollback activity and operator or synthetic traffic described in the first-day report. Script-level aggregation does not isolate deployment versions, the nonce middleware's own CPU, or the cause of traffic changes. Static-cache and Pagefind recovery work remain separate.

Workers Paid remains **user-confirmed**, not newly verified billing metadata. The operations review's quota scenarios and pricing references are dated context, not a fresh invoice or remaining-allowance check. This observation does not establish actual charges, aggregate billed CPU, a cost forecast or a zero-cost guarantee, and does not silently replace the Paid plan with a hypothetical Free quota.

## Disposition

The requested 48-hour operational collection and first-day requery are delivered. Reported invocation errors remain zero, observed CPU quantiles are recorded, and the bounded public HTTP sample retains the expected nonce/cache headers. **Do not claim complete cost/latency acceptance.**

Root reviewed the original criteria and retained `9lk` **blocked specifically on the unmatched pre/post public HTML latency comparison**: elapsed time, additional unmatched probes or preview CPU cannot reconstruct a missing comparable baseline. Actual billing evidence also remains unavailable and no charge claim is made. The completed observation does not close or redefine the original criteria.

Resolving that evidence gap needs an authoritative existing comparable baseline or an explicitly agreed, valid alternative acceptance method. This document authorizes neither a live experiment, rollback/redeployment for measurement, paid monitoring nor additional production changes.
