<section class="quote-section">
  <div class="quote-inner reveal">
    <div class="quote-mark">&ldquo;</div>
    <p class="quote-text">A journey is best measured in friends, rather than miles.</p>
    <div class="quote-author">Tim Cahill</div>
  </div>
</section>

<style>
  .quote-section {
    padding: 180px 40px;
    position: relative;
    overflow: hidden;
  }

  .quote-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 50%, rgba(196, 86, 42, 0.04) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 50%, rgba(46, 109, 121, 0.04) 0%, transparent 50%);
  }

  .quote-inner {
    position: relative;
    max-width: 900px;
    margin: 0 auto;
    text-align: center;
  }

  .quote-mark {
    font-family: var(--font-display);
    font-size: 120px;
    line-height: 1;
    color: var(--color-terracotta);
    opacity: 0.15;
    margin-bottom: -40px;
  }

  .quote-text {
    font-family: var(--font-display);
    font-size: clamp(28px, 4vw, 48px);
    font-weight: 400;
    font-style: italic;
    line-height: 1.4;
    color: var(--color-ink);
    margin-bottom: 40px;
  }

  .quote-author {
    font-size: 14px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-text-muted);
    font-weight: 500;
  }

  .quote-author::before {
    content: '';
    display: inline-block;
    width: 30px;
    height: 1px;
    background: var(--color-text-muted);
    vertical-align: middle;
    margin-right: 16px;
  }

  @media (max-width: 1024px) {
    .quote-section { padding: 120px 24px; }
  }
</style>
